/*
package io.digikraft.data.repository.remote

import io.digikraft.domain.model.vendor.VendorProductEntity
import io.digikraft.domain.datasource.IRestDataService
import javax.inject.Inject

class RestDataRepository @Inject constructor(private val restDataService: IRestDataService) :
    IRestDataService {

    override suspend fun vendorList(): Result<List<VendorProductEntity>> {
        return restDataService.vendorList()
    }
}*/
